<Component name="EntityAgentNode">
  <ModalDialog id="eaHelp" title="Kleindorfer's Agent">
    <Text>A software agent that acts on behalf of the entity to negotiate and record agreements.</Text>
  </ModalDialog>
  <ModalDialog id="eaMagnet" title="Kleindorfer's Agent Terms">
    <Text>This magnet represents the entity agent acceptance criteria. When attracted, it confirms the entity willingness to bind to the offered terms.</Text>
  </ModalDialog>
  <VStack gap="$space-1" padding="$space-2">
    <HStack horizontalAlignment="start">
      <Text fontSize="2rem" color="$color-primary" cursor="pointer" onClick="eaMagnet.open()">⊃</Text>
    </HStack>
    <HStack verticalAlignment="center" horizontalAlignment="center" gap="$space-2">
      <Icon name="bot" width="24px" height="24px" />
      <Text fontWeight="bold" fontSize="$fontSize-lg">Kleindorfer's Agent</Text>
    </HStack>
    <Text fontSize="$fontSize-small" color="$textColor-secondary" when="{phase < 5}">No term offered yet</Text>
    <Text fontSize="$fontSize-small" fontWeight="bold" color="$color-primary" when="{phase >= 5 && phase < 6}">Received: {offeredTerm}</Text>
    <Text fontSize="$fontSize-small" fontWeight="bold" color="$color-success" when="{phase >= 6 && lookupTermPolicy(offeredTerm) === 'Accept'}">Accepted: {offeredTerm}</Text>
    <Text fontSize="$fontSize-small" fontWeight="bold" color="$color-danger" when="{phase >= 6 && lookupTermPolicy(offeredTerm) === 'Reject'}">Rejected: {offeredTerm}</Text>
  </VStack>
</Component>
